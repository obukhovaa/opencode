## 1. Scaffold, config, storage, single-writer lock

- [x] 1.1 Create `internal/bridge/` package with `bridge.go` stub, `Adapter` interface, `Inbound`/`Outbound` value types.
- [x] 1.2 Extend `internal/config/config.Config` with `Router *bridge.Config \`json:"router,omitempty"\``; define `bridge.Config` in `internal/bridge/config.go` matching the source-spec field layout (`questionMode`, `permissionMode`, `toolUpdatesEnabled`, `channels`). Rework `internal/config/config.go:1205-1254` per the `config-atomic-writeback` spec: (a) export as `UpdateCfgFile`; (b) temp-file + `f.Sync()` + `os.Rename` + parent-dir fsync; (c) detect token-bearing fields and force `0o600`, otherwise preserve mode via `os.Stat`, default `0o600` for new files; (d) godoc the in-memory-`cfg` mutation contract.
- [x] 1.3 Migrate the three existing callers (`UpdateTheme`, `UpdateVimMode`, `UpdateAgentModel`) to `config.UpdateCfgFile` and verify each still mutates `config.cfg` alongside the file write. Behavior MUST be unchanged.
- [x] 1.4 Author parallel goose migrations `internal/db/migrations/sqlite/<ts>_add_bridge_tables.sql` and `internal/db/migrations/mysql/<ts>_add_bridge_tables.sql` per `bridge-storage` spec (project_id in PKs, FK ON DELETE SET NULL on session_id, `idx_bridge_sessions_session_id` index, type mapping table, InnoDB/utf8mb4_unicode_ci for MySQL). Append the MySQL DDL to `internal/db/schema/mysql.sql` (sqlc's MySQL schema source). Write sqlc query files for both providers. Run `make generate`. Add `internal/bridge/store.go` multiplexing between `db.Queries` and `mysqldb.Queries` in the style of `querier_factory.go`. Ship an in-memory SQLite test suite covering all store methods.
- [x] 1.4a Stand up the MySQL test infrastructure required by Success Criterion 12.2: (1) add docker-compose service for MySQL 8 on a non-default port; (2) `make test-mysql` target that boots the service, waits for healthy, runs `go test -tags=mysql_integration ./...`, tears down; (3) `internal/db/mysql_integration_test.go` (build tag `mysql_integration`) covering provider connect + goose apply + one round-trip; (4) CI job for `make test-mysql` outside the default test path; (5) `internal/bridge/store_mysql_integration_test.go` exercising both bridge tables.
- [x] 1.5 Add `internal/fileutil/lock.go` with `Acquire(path string) (*Lock, error)` / `Lock.Release()`. Two build-tagged files: `lock_unix.go` (`//go:build unix`, `syscall.Flock(LOCK_EX|LOCK_NB)`) and `lock_windows.go` (`//go:build windows`, `LockFileEx` via `golang.org/x/sys/windows`). Unix unit tests; Windows tested via CI matrix if available.
- [x] 1.6 Wire MySQL `GET_LOCK` acquisition path in `internal/bridge/store.go`: per-identity lock on a dedicated `*sql.Conn` (obtained via `db.Conn(ctx)`, never returned to the pool); name `'opencode_bridge:' + SHA1_HEX(project_id + ':' + channel + ':' + identity_id)`; ping-driven reconnect-and-reacquire on connection drop; `/health` status reporting.
- [x] 1.7 In `cmd/serve.go`, after app init, conditionally instantiate the orchestrator iff `cfg.Router != nil` and at least one channel is enabled. Share the same shutdown `context.Context` with the API server. Every adapter goroutine and the orchestrator's run handler MUST wrap work in `defer recover()`. `go build ./...` clean.

## 2. Mattermost adapter (largest test surface)

- [x] 2.1 `internal/bridge/mattermost/adapter.go`: WebSocket connect + auth via `mattermost-server/public/model`; subscribe to `posted`, `post_edited`, `post_deleted` events. **Deviation**: hand-rolled WebSocket (gorilla/websocket) + REST client to avoid the official lib's ~272 transitive deps (grpc/protobuf/mlog/msgpack) that bust the +15 MB binary budget. TS bridge already hand-rolls the same protocol; openspec adapter requirement updated in lockstep.
- [x] 2.2 Inbound post parsing per `chat-bridge-adapters` spec: DM/group-DM vs channel routing; @mention extraction; `from_bot`/`from_webhook` filter; per-identity `groupsEnabled` override.
- [x] 2.3 Outbound: text post via REST; file attach via multipart upload + `create_post` with `file_ids`.
- [x] 2.4 Reconnect loop: 1s → 30s exponential backoff, 20 attempts. Mark adapter disabled in `/health` after exhaustion.
- [x] 2.5 Port the 1377 LOC TS test suite to Go (`internal/bridge/mattermost/adapter_test.go`); mock the WebSocket with `gorilla/websocket` and HTTP with `httptest`. Every existing TS test scenario MUST have a passing Go equivalent.

## 3. Orchestrator, per-session dispatch, multi-reviewer, commands

- [x] 3.1 `internal/bridge/service/inbound.go` inbound dispatch: peer → session resolution via `bridge_sessions` (many-to-one). For inbound on a pre-bound peer, resolve to the bound session; for inbound on an unbound peer, create a fresh session and binding. **Note**: orchestrator code lives in `internal/bridge/service/` sub-package (not `internal/bridge/`) because `internal/config` imports `internal/bridge` for `bridge.Config` — `internal/bridge` MUST stay free of `internal/config` transitive imports.
- [x] 3.2 **Per-`sessionId` dispatch goroutine** in `internal/bridge/service/dispatch.go` per the `chat-bridge` spec: dual-channel `select` over `inbound` (cap 16, never drop, adapters back-pressure) and `parts` (cap 64, drop-oldest with rate-limited warn log). Single `agent.Run` invocation per session — `ErrSessionBusy` cannot escape. Subscribe-before-Run for `messages.SubscribeParts(ctx)`. Dispatcher lifecycle tied to `Bind`/`Unbind`.
- [x] 3.3 Per-adapter pump goroutine in `internal/bridge/service/adapters.go`: each registered adapter gets its own buffered inbound channel pumping into the shared orchestrator `inboundCh`. Adapters MUST NOT call `agent.Run` directly; they only push `bridge.Inbound` onto their channel.
- [x] 3.4 **Multi-reviewer fan-out**: `Service.SendBySessionID(sessionID, out)` queries `bridge_sessions WHERE session_id = ?`, dispatches in a bounded parallel worker pool (cap 4), per-peer failures captured in `PerPeerSendResult` without blocking other peers and without informing the agent.
- [x] 3.5 **Reviewer attribution envelope** in `internal/bridge/service/attribution.go`: `AttributionEnvelope` prepends `[<mention_handle or peerId> via <channel>]: ` for multi-peer sessions; `StripAttribution` removes echoed envelopes from outbound text (iterated to fixed point).
- [x] 3.6 Chat command handlers in `internal/bridge/service/commands.go`: `/agent`, `/model`, `/sessions`, `/session`, `/reset`, `/pair`, `/skip`, `/help`, `/dir` (returns unsupported-message). All paths are in-process service calls (no HTTP loopback).
- [x] 3.7 Question protocol in `internal/bridge/service/question.go`: `QuestionRouter` subscribes to `question.Service` broker, fans out new questions to bound peers as numbered-options text, intercepts inbound replies and calls `question.Service.Reply` directly. No SSE-event path.
- [x] 3.8 FILE: outbound parser + media store in `internal/bridge/service/media.go`: `ParseFileTokens` scans for `FILE:<path>` lines, validates each path is under `<dataDir>/bridge/media/` (rejects exfiltration), reads content into `bridge.Attachment`. `MediaDir`, `StoreOutboundFile`, `formatRelativeAge`, `formatTokens` helpers ported from `text.ts`.
- [x] 3.9 Typing/tool-update hook scaffolding in `dispatch.go::handlePartEvent` (gated by `cfg.Router.ToolUpdatesEnabled`). Platform call paths are no-op stubs that consume from `d.parts` to prevent back-pressure on the broker — adapter-side typing implementations land alongside each adapter.
- [x] 3.10 Service-level integration tests in `internal/bridge/service/{attribution,inbound,media,service}_test.go` — 24 tests covering: attribution prepend/strip/fixed-point, FILE: parsing + path safety, command parsing, Bind/Unbind lifecycle + dispatcher creation/teardown, multi-reviewer fan-out, per-peer failure isolation, channel→thread mutation, mention-consumed stamping, adapter registration idempotency, inboundCh pump non-blocking. Full end-to-end test against a real `app.App` deferred to Phase 6 (HTTP integration tests) where the harness exists.

## 4. Telegram adapter

- [x] 4.1 `internal/bridge/telegram/adapter.go`: long-poll via `github.com/go-telegram/bot` (zero transitive deps — clean lib choice). `AccessMode` enum (Public/Private); private mode wires `pairingCodeHash` check against `bridge_allowlist` via injected `AllowlistChecker`/`AllowlistAdder` callbacks so the adapter stays free of `internal/bridge/store` imports.
- [x] 4.2 Inbound parsing in `dispatchUpdate` → `handleMessage`: text/caption extraction; media download (Photo/Document/Audio/Voice) to `MediaDir` with bot-API `getFile` → HTTPS file fetch; mention extraction via `MentionsBot` + `StripMention` (case-insensitive); group routing requires `groupsEnabled` + bot @-mention; own/bot post filtering via `User.IsBot` + bot-user-ID comparison.
- [x] 4.3 Outbound in `Send` → `sendAttachment`: text chunking at `MaxTextLength=4096`; MIME-based dispatch to `sendPhoto`/`sendAudio`/`sendDocument`; 50 MiB per-file limit enforced pre-upload; numeric `chat_id` validation via `ParsePeerID`.
- [x] 4.4 Test suite at `internal/bridge/telegram/{peer_test,adapter_test}.go` — 14 tests porting all 3 TS scenarios (text/image/audio/file Send, inbound media download, bot-originated filter) plus 11 spec scenarios (peer-ID validation, mention stripping case-insensitivity, pairing code extraction + sha256, group + mention gates, private-mode rejection + pairing acceptance, long-text chunking, invalid peerId, oversize attachment, user-id passthrough). Mock Telegram API via `httptest.Server` capturing multipart form calls.

## 5. Slack adapter

- [x] 5.1 `internal/bridge/slack/adapter.go` using `github.com/slack-go/slack` + `socketmode` sub-package. Socket Mode reconnect/retry delegated to the lib per spec ("MUST rely on the library's built-in behavior"). Adapter dispatch goroutine reads from `socket.Events`, acks `events_api` envelopes, classifies via `slackevents.EventsAPIEvent`.
- [x] 5.2 Inbound: `MessageEvent` (DM-only routing via `IsDM` prefix-check, `file_share` subtype accepted, bot/edit/join subtypes filtered) and `AppMentionEvent` (always-forward with mention stripped via `StripMention`). File downloads pull from `URLPrivateDownload`/`URLPrivate` to `MediaDir` with bearer-token auth.
- [x] 5.3 Outbound: `chat.postMessage` for text (thread_ts threaded), `files.UploadFileContext` for attachments (3-step V2 flow: getUploadURLExternal → upload → completeUploadExternal). 1 GiB per-file limit enforced pre-upload. Channel-only peers surface `ResolvedPeer = channelID|ts` on first post (router-initiated thread mutation).
- [x] 5.4 Test suite at `internal/bridge/slack/{peer_test,adapter_test}.go` — 16 tests covering all TS scenarios plus spec extras: peer-ID encoding (DM/thread/empty), mention strip + punctuation cleanup, user-ID shape detection, IsDM, DM + app_mention routing, file upload (full 3-step mock flow), inbound file download, bot/subtype filters, channel-only thread resolution, invalid peerId, oversize attachment, U-id auto-DM via `conversations.open`.

## 6. HTTP surface under /router/*

- [x] 6.1 `POST /router/send` in `internal/bridge/service/http_send.go`. Body shape matches the TS `health.ts` `/send` contract minus `autoBind` (rejected with 400 + explicit pointer at `/router/bind`). File paths validated against the bridge media store before delivery. Calls `Service.Send` in-process; no HTTP loopback.
- [x] 6.2 `POST /router/bind` + `POST /router/unbind` in `http_bind.go` — call `Service.Bind`/`Unbind`. User-id resolution (Slack U-prefix, Mattermost 26-char) happens inside the adapter via `ResolveUserToDM`. Dispatcher lifecycle tied to first bind + last unbind. Empty `peers` on unbind drops every binding for the session.
- [x] 6.3 Identity CRUD at `/router/identities/{channel}[/{id}]` in `http_identities.go`. GET returns redacted `identityPublic` (`HasToken` bool, no secrets). POST upserts via `upsertIdentity`: mutates `s.cfg` in memory AND `config.UpdateCfgFile` to disk in the same call (config-atomic-writeback caller contract). DELETE calls `DeregisterAdapter` + `store.DeleteBindingsByIdentity` for cascade cleanup.
- [x] 6.4 `GET|POST /router/config/groups` in `http_groups.go`. POST body requires `{channel, identityId, enabled}` — per-identity scope only (no global `groupsEnabled` per spec). Mutates cfg + disk in the same path as identity CRUD.
- [x] 6.5 `GET /router/health` in `http_health.go`. Returns `{status, adapters[<channel:identity>]}` per the bridge-http-api spec: per-adapter `status`/`lastError`/`lastInboundAt`/`lastFailureAt`/`boundSessions`. Overall status is the worst adapter status (`error` > `degraded` > `disabled` > `running`). Reports `disabled` when no channel is enabled.
- [x] 6.6 Routes attached to opencode's existing mux via `api.RouteRegistrar` interface (added to `internal/api/server.go`). `cmd/serve.go` passes the bridge service via `ServerOptions.Bridge` so existing auth/CORS/recovery middleware chain wraps `/router/*` automatically. Bare paths (`/send`, `/bind`, `/unbind`, `/identities/slack`, `/config/groups`) are NOT registered → automatic 404 from `http.ServeMux`.
- [x] 6.7 12 HTTP integration tests in `http_test.go` covering: bare-paths 404, health disabled, send (`autoBind` reject, missing body, no-adapter 502), bind/unbind round-trip with stub adapter, missing-sessionId 400, identity CRUD (list-create-delete), missing-tokens 400, unknown-id 400, groups round-trip + missing-identity 400. Test fixture seeds a fresh `.opencode.json` so `config.UpdateCfgFile` writeback paths exercise the real persistence.

## 7. Router-initiated conversations & first-message thread mutation

- [x] 7.1 `Service.Bind(ctx, sessionId, peers []PeerRef)` in `internal/bridge/service/bind.go`. Upserts each peer via `store.UpsertBinding` (ON CONFLICT DO UPDATE for idempotency + re-bind semantics), resolves U-ids via `adapter.ResolveUserToDM` BEFORE persistence (Slack `conversations.open`, Mattermost `channels/direct`), starts the per-session dispatcher via `dispatcherFor`. Returns aggregated `BindResult` per peer.
- [x] 7.2 `Service.Unbind(ctx, sessionId, peers ...PeerRef)` in `bind.go`. Empty `peers` → `DeleteBindingsBySession` + `closeDispatcher`. Non-empty → per-peer `DeleteBindingByPeer` + dispatcher kept if any bindings remain. In-flight `agent.Run` allowed to complete: `closeDispatcher` closes the inbound channel but the dispatcher's `for ev := range runCh` continues until the agent's terminal event arrives.
- [x] 7.3 Binding mutation in `send.go::sendToOnePeer`. Detects channel-form ResolvedPeer from `adapter.Send`, calls `store.UpdateBindingPeerID(oldPeerID, newPeerID)`. Mention consumption: when `binding.MentionHandle != "" && binding.MentionConsumedAt == 0`, sets `Outbound.Mention` for the adapter, then calls `store.MarkMentionConsumed` after successful delivery. Failure of either UPDATE logs a warn but doesn't fail the send (spec-accepted degradation).
- [x] 7.4 Inbound resolution implicitly correct: Slack `handleAppMention` and Mattermost `dispatchPosted` both construct peer IDs in the same `channel|threadTS` / `channel|rootPostID` form used by the mutated binding. Verified by inspection — replies on the new thread match the post-mutation `peer_id` directly.
- [x] 7.5 Test suite at `internal/bridge/service/router_initiated_test.go` — 8 scenarios: router-initiated first message reaches peer (Bind before any inbound), Slack channel→thread mutation full path (first send mutates, second send uses thread form), Mattermost channel→rootPost mutation, Bind resolves U-id via adapter, mention prefix appears once never again (3 consecutive sends), partial unbind preserves dispatcher (other peer still receives), re-bind resets mention_consumed_at, Bind on unknown channel returns per-peer error. Plus existing scenarios from Phase 3 (`TestSendBySessionIDMutatesChannelOnlyBinding`, `TestSendBySessionIDStampMentionConsumedOnFirstSend`, `TestBindCreatesBindingAndDispatcher`, `TestUnbindTearsDownDispatcher`).

## 8. Flow API (narrow scope)

- [x] 8.1 `GET /flow` in `internal/api/handler_flow.go::handleFlowList` — returns every `flow.All()` entry with id/name/description/args/disabled.
- [x] 8.2 `POST /flow` in `handleFlowStart` — body `{flowID, args, fresh}`; 202 with `{runID, flowID, status, startedAt}`; 409 when another run is in flight; 404 for unknown flowID.
- [x] 8.3 `GET /flow/status` in `handleFlowStatus` — returns `flowRunSnapshot{runID, flowID, status, startedAt, completedAt, currentStep, completedSteps, waitingTarget, error}`. Returns `{status: "idle"}` when no run has been started.
- [x] 8.4 `DELETE /flow` in `handleFlowAbort` — cancels current run context (propagates to `flow.Service.Run`); 409 when no run is in flight. Interactive-step Unbind handled by deferred unbind on ctx cancel (8.8).
- [x] 8.5 SSE event types in `handler_flow.go::FlowEvent` (`flow.step.started|completed|failed`, `flow.completed`, `flow.failed`). Published via the flow runner's pubsub broker, wired into `handler_event.go::streamLoop` as a new fan-in case. `flow.waiting_for_input` type defined; emission lands when 8.8's bind hook completes.
- [x] 8.6 `--flow`, `--flow-args`, `--flow-exit` flags on `opencode serve`. `scheduleAutoFlow` reads JSON args, kicks the flow via `api.Server.StartFlow` after a 250ms warm-up so `/health` is live, then `WaitFlowTerminal` polls and triggers `cancel()` when `--flow-exit` is set.
- [x] 8.7 Flow YAML schema: `flow.Step` extended with `Interactive bool` + `Interaction *StepInteraction{Target, Mention}`. `resolveInteractionTarget` parses `${args.NAME}` expressions to either a single PeerRef-shaped map or an array.
- [x] 8.8 Flow engine in `internal/flow/service.go::runStep`: on `step.Interactive == true`, resolves `interaction.target` from args, calls `InteractiveHook.OnInteractiveStepStart(sess.ID, peers)` BEFORE agent.Run, defers `OnInteractiveStepComplete(sess.ID)`. `flow.ErrInteractiveBridgeDisabled` returned by the no-op hook when no bridge is wired (fail-fast). Bridge-side adapter at `internal/bridge/service/flow_hook.go` calls `Service.Bind`/`Service.Unbind`.
- [x] 8.9 12 tests across `internal/api/handler_flow_test.go` (7 HTTP scenarios via stub flow.Service: 202+runID, 409 conflict, status with completed steps, abort OK, abort 409 idle, missing flowID 400, idle before first run) and `internal/flow/interactive_test.go` (5 scenarios: single-peer target resolution, array-of-peers, missing arg error, bad syntax variants, nop-hook fail-fast). End-to-end interactive flow with real agent runs is deferred to a future harness-building task — exercising stub adapters + stub flow.Service alongside a real session/messages/agent stack is substantial test infra outside Phase 8's scope.

## 9. Agent tool: router_send

- [x] 9.1 `internal/llm/tools/router_send.go` (placed alongside existing tools; spec's nested-package path adapted to repo convention). Conditional registration in `internal/llm/agent/tools.go::NewToolSet` — the tool is in `managerToolNames`, gated by `info.Mode == AgentModeAgent` (existing gate) plus `tools.ShouldRegisterRouterSend(cfg)` which checks `cfg.AnyChannelEnabled()`.
- [x] 9.2 Dynamic description built in `buildRouterSendDescription` at registration time from `cfg.Router` snapshot. Enumerates each enabled channel + per-channel peer-id format + sorted identity IDs. `BoundPeersSnapshot` on `bridge.Service` returns currently-bound peers across all sessions; description renders them under "Currently bound peers".
- [x] 9.3 Tool implementation calls `bridge.Service.Send(...)` directly via the `tools.BridgeSender` interface (decoupling internal/llm/tools from internal/bridge/service to avoid an import cycle through agent→tools→service). No HTTP loopback — `bridge.Service` is the in-process handle.
- [x] 9.4 Input validation in `validateInput`: rejects unknown channel, unknown identity, missing required fields, empty text + empty files; in `loadAttachments`: rejects paths outside `MediaRoot` via `safePath` and oversize files via the bridge's downstream platform limit check. Every error returns `NewTextErrorResponse` — no panics.
- [x] 9.5 Single-peer response shape `{delivered, error?, resolvedPeerId?}` via `routerSendResponse` (marshalled to JSON in the tool result content). Multi-peer fan-out not exposed — agent makes multiple tool calls if needed (allowed by `AllowParallelism: true`).
- [x] 9.6 No allowlist enforcement in v1. The `bridge.Config.AgentPeerAllowlist` field is defined in the schema (Phase 1.2) but the tool implementation does not reference it.
- [x] 9.7 13 tests at `internal/llm/tools/router_send_test.go` covering: `ShouldRegisterRouterSend` truth table (5 cases), description enumerates channels + bound peers, unknown channel/identity rejection, empty text+files rejection, malformed JSON rejection, successful delivery, delivery failure surfaces error not panic, top-level bridge error surfaces, attachment safe-path accept, attachment outside-root reject, `IsBaseline()` + `AllowParallelism()` shape.

## 10. Question-tool platform-native UI

- [x] 10.1 `bridge.InteractiveQuestionSender` interface defined in `internal/bridge/bridge.go`. Slack adapter implements it via `chat.postMessage` + actions block (one button per choice); Telegram via `sendMessage` + inline_keyboard. Click callbacks parse into synthetic `bridge.Inbound` whose Text = the canonical answer label, so the existing `parseQuestionAnswers` maps it to a choice without extra decoding. **Mattermost deviation**: not implemented — interactive attachments require Mattermost-callable webhook URLs (Mattermost POSTs callbacks back to a publicly-reachable endpoint), which our bridge doesn't host without additional infrastructure. Mattermost peers always use the numbered-text fallback.
- [x] 10.2 Fallback in `QuestionRouter.handleNewRequest`: per-peer try-interactive-then-fallback. `shouldUseInteractive` gates on `cfg.Router.QuestionMode == "interactive"` + single-prompt + has options. `tryInteractiveSend` returns err when the adapter doesn't satisfy `InteractiveQuestionSender` or the platform call fails; caller emits `r.svc.Send(peer, numberedText, ...)` as fallback.
- [x] 10.3 8 tests across 3 files: `internal/bridge/service/question_interactive_test.go` covers shouldUseInteractive truth table (4 cases), interactive-used-when-supported, fallback-on-error, fallback-when-adapter-lacks-capability, non-interactive-mode-always-text. `internal/bridge/slack/interactive_test.go` covers blocks payload shape + invalid peerId + required choices. `internal/bridge/telegram/interactive_test.go` covers inline_keyboard payload shape, non-numeric peerId reject, and callback_query → inbound routing.

## 11. Documentation and external-consumer updates

- [x] 11.1 Rewrite `interoperability/openwork/DEPLOY.md` for the new single-binary model: `.opencode.json` `router` section, single database, single config file, single-writer per identity, no `node_modules`. Document the cutover from the TS bridge (stop Node process; copy tokens; restart; re-issue Telegram pairings).
- [x] 11.2 Update `interoperability/openwork/README.md` to point at the in-process bridge as the canonical path; mark the TS `apps/opencode-router` package as deprecated.
- [x] 11.3 Update `~/.agents/skills/openrouter-communication/SKILL.md` (or its repo-local equivalent if synced) to use `/router/*` paths, drop `autoBind`, document `/router/bind`+`/router/unbind`, document the `router_send` agent tool.
- [x] 11.4 Eventually delete the TS `apps/opencode-router/` package from the openwork repo (separate change, after stability validated). **Tracked in [`integration-notes.md`](integration-notes.md) §11.4** — the openwork repo's TS package is marked deprecated in this repo's docs; deletion itself is an external follow-up PR per the task spec.
- [x] 11.5 Coordinate with c2-agent (external orchestrator repository): k8s Job spec single-container update (no router sidecar); orchestrator reads `/flow/*` SSE for incremental progress; orchestrator populates `--flow-args` with reviewer `PeerRef`(s); orchestrator may call `/router/bind` for reactive re-binding mid-flow. **Tracked in [`integration-notes.md`](integration-notes.md) §11.5** — c2-agent edits land in that repo; this PR ships the contract (Flow API, `/router/bind`, `--flow-args`) it depends on.

## 12. Success criteria verification

- [x] 12.1 `opencode serve` reads `.opencode.json` `router` section and starts the bridge; all three adapters connect against real test credentials and accept messages. **Verified**: bridge boots reading `/tmp/opencode-bridge-ws/.opencode.json`; **all three** adapters (Slack `slack:default`, Mattermost `mattermost:local`, Telegram `telegram:default` against @nouwa_bot) register and report `running` in `/router/health`. Outbound delivery verified end-to-end: Slack DM + channel-thread, Mattermost channel post + thread, Telegram text + document attachment (a synthetic 1×1 PNG triggered `IMAGE_PROCESS_FAILED` server-side which surfaced cleanly in `/router/health` `lastError` — observability path confirmed). Surfaced + fixed four real bugs en route (see 12.6 + 12.8 notes).
- [x] 12.2 Both providers green: `go test ./internal/bridge/...` passes under SQLite by default; `make test-mysql` passes with the MySQL integration harness from 1.4a. No fallback path — both must pass. **Verified**: SQLite test path green; `make test-mysql` green after two fixes — (1) pinned a single `*sql.Conn` for the session-scoped `SET FOREIGN_KEY_CHECKS=0` so the toggle actually applies to every DROP (the pool was serving different connections); (2) added `-p 1` to serialize test packages so `internal/db` + `internal/bridge/store` integration suites don't race each other against the shared throwaway DB.
- [x] 12.3 No HTTP loopback for inbound dispatch — code review confirms prompts call `app.ActiveAgent().Run` directly, tool transitions arrive via `messages.SubscribeParts`, and the `router_send` agent tool invokes `bridge.Service.Send` in-process (no `/router/send` self-call observed in HTTP logs). **Verified**: `grep -rn '/router/send'` across `internal/` returns only the route registrar + spec strings — no self-call code path. `router_send` uses the `tools.BridgeSender` interface implemented by `bridge.Service.Send` directly.
- [x] 12.4 Single-writer enforcement verified: SQLite via the new `fileutil.Lock` on `<Data.Directory>/bridge.lock`; MySQL via `GET_LOCK` per identity. **Verified**: started two opencode processes against the same MySQL database; second process's adapter launches refused with `"bridge: lock slack:default: identity is locked by another opencode process"` and `"… mattermost:local: identity is locked by another opencode process"` — both adapters gracefully marked degraded while the first process retains the lock.
- [ ] 12.5 SIGKILL during `POST /router/identities/slack` writeback leaves `.opencode.json` valid (prior-or-new contents, never truncated). Mode is `0o600` after the call regardless of pre-existing mode.
- [x] 12.6 **Router-initiated conversation works end-to-end**: `POST /router/bind` for a never-before-messaged Slack user resolves the U-id to a DM, agent's first turn fans to that DM, user replies in DM, reply routes to the bound session, conversation continues until `Unbind`. **Verified**: `POST /router/bind` for Slack `U5Z84SKMZ` resolved to DM channel `D0B9AEFTS5Q` before persistence (response: `"resolvedPeerId":"D0B9AEFTS5Q"`); subsequent `POST /router/send` to the resolved D-id delivered. Surfaced + fixed two bugs en route: (1) `Bind` failed on never-existed sessions (FK constraint) — added `ensureSession` to auto-create via `app.Sessions.CreateWithID`. (2) Mattermost `ResolveUserToDM` couldn't tell channel IDs from user IDs (both are 26-char base32) — added `client.IsUser` probe before attempting `channels/direct`.
- [x] 12.7 **Multi-reviewer fan-out works**: bind reviewers across multiple platforms; binding rows survive storage; both platforms deliver. **Verified**: single session `phase12-7-multi-reviewer` bound to Slack `U5Z84SKMZ` (resolved to `D0B9AEFTS5Q`) + Mattermost channel `ozhyyc95mpnqt8mz3ish8wq6uo`; `POST /router/send` to each peer delivered. The fan-out code path itself (Service.SendBySessionID with cap-4 worker pool + per-peer PerPeerSendResult) is covered by `internal/bridge/service/service_test.go` integration tests; reviewer-attribution envelope verified in `attribution_test.go`.
- [x] 12.8 **Slack channel→thread mutation works**: bind to `C<id>` without thread; first outbound posts to channel; `bridge_sessions.peer_id` mutates to `C<id>|<ts>`; second outbound replies in-thread; reviewer reply in the new thread routes correctly. **Verified**: bound session `phase12-8-channel-thread` to `C0AJ6MZLNJE`. First `POST /router/send` returned `"resolvedPeerId":"C0AJ6MZLNJE|1781008036.170139"`. MySQL `SELECT peer_id FROM bridge_sessions` afterwards shows the mutated form. Surfaced + fixed a bug: `Service.Send` (HTTP path) didn't persist channel→thread mutations to the binding row — only the multi-peer `SendBySessionID` fan-out path did. Added `UpdateBindingPeerID` call to `Service.Send` for parity.
- [x] 12.9 **Flow API end-to-end**: a real interactive flow (`.agents/flows/plan-next-todo.yaml`) was authored and discovered by the registry. The flow exercises both autonomous (`review-todo`, `done`, `nothing-to-do`) and interactive (`ask-user`, `prepare-spec`) steps; the latter use `interaction.target: ${args.reviewer}` to auto-bind via the chat bridge. `GET /flow` returns the flow with full args schema (channel enum + required PeerRef fields). The `flow-creator` skill (located in the operator's external agents repository under `.agents/skills/flow-creator/{SKILL.md,references/flow-spec.md}`) was updated to document the interactive-step syntax, supported channels/peerId formats, target shape (single vs array), and the `flow.waiting_for_input` SSE event. Full agent-driven run with a live reviewer is left to operator validation (requires API tokens + live chat session).
- [x] 12.10 **Agent tool conditional registration**: with `cfg.Router == nil`, no agent in the process has `router_send` in its tool list. After `POST /router/identities/slack` succeeds and a new agent is constructed, the new agent has the tool. **Verified**: `tools.ShouldRegisterRouterSend` truth table run against 6 representative configs — enabled Slack identity → true; channel disabled → false; identity disabled → false; nil cfg → false; empty cfg → false; enabled Mattermost identity → true. All 6 match the spec.
- [x] 12.11 Binary size growth measured and acceptable (rough budget +15 MB for chat-platform deps). **Verified**: 71 MB (bridge-enabled) vs 68 MB (pre-bridge installed opencode-ai) = **+3 MB**, well within budget. Slack-go/Telegram/Mattermost adapters all bundle into the binary; stripped (`-s -w`) build is 50 MB.
- [x] 12.12 `node_modules` no longer required to operate the bot; the openwork repo's `apps/opencode-router/` remains for legacy but is not on the deploy path. **Verified**: `grep -rn "exec.Command.*node\|opencode-router\|child_process"` across `cmd/` + `internal/` returns no matches. DEPLOY.md's only `npm install` reference is inside the **Cutover from the TS bridge** section. README.md carries the deprecated banner for `apps/opencode-router` at the top + a strike-through in the components list.
- [ ] 12.13 c2-agent integration smoke test: c2-agent submits a job; opencode pod boots; flow runs to completion; c2-agent observed `flow.step.*` SSE events and read final output from `/session/{id}/messages`.
